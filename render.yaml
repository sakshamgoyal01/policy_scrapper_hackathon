services:
  - type: web
    name: hackrx-webhook
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: ./start.sh
    branch: main
    runtime: python
